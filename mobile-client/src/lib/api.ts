/**
 * HTTP client setup.
 * Axios instance with JWT Authorization interceptors.
 * Endpoints follow kebab-case. Payloads use camelCase.
 *
 * TODO (Epic 2 - Auth): configure baseURL from env, attach/refresh JWT tokens.
 */
